apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: messenger
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ZhenyaHeisenberg/lab-k8s-messager.git
    targetRevision: main
    path: k8s/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: messenger-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ApplyOutOfSyncOnly=true
      - PruneLast=true